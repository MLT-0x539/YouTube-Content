#include <stdio.h>

int main(void) {
 int a,b;

for (a = 0; a <= 10; a++) {
   printf("Value is: %d\n", a); 
     // Counts up from 0 to 10
 }

printf("\n\n");

for (b = 10; b >= 0; b--) {
   printf("Value is: %d\n", b);
     // Counts down from 10 to 0
 }

return(0);

}
