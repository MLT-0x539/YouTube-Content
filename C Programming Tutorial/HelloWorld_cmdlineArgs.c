#include <stdio.h>

int main(int argc, char* argv[]) {
  // Overview of how we can specify command-line arguments via int main()
  // cmdline inputs assigned via argc/argv 
  printf("Hello World!\n");
  return(0);
}
